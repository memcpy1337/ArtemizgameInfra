nameOverride: auth-service
replicaCount: 1

image:
  repository: ghcr.io/memcpy1337/artemizgameauth
  digest: "sha256:e4dfa92aa24e3660b2d44bf5b18836ec3308029ce94a81d109d3b17cf5b59025"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  ports:
    - name: http
      port: 6023
      targetPort: 6023
      protocol: TCP
    - name: https
      port: 6024
      targetPort: 6024
      protocol: TCP

ingress:
  enabled: false

envFrom:
  - secretRef: jwt-secrets
  - secretRef: common-secrets
  - secretRef: auth-database-secret

env:
  ASPNETCORE_ENVIRONMENT: "Production"
  ASPNETCORE_URLS: "http://+:6023"
  ASPNETCORE_HTTP_PORTS: "6023"
  ASPNETCORE_HTTPS_PORTS: "6024"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi