nameOverride: auth-service
replicaCount: 1

image:
  repository: ghcr.io/memcpy1337/artemizgameauth
  digest: "sha256:bb9f11c14cc3400b58ed5cf7c4dfe02ddc8a9cb382093360a7f2882f7ba9d27f"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  ports:
    - name: http
      port: 6023
      targetPort: 6023
      protocol: TCP
    - name: https
      port: 6024
      targetPort: 6024
      protocol: TCP

ingress:
  enabled: false

envFrom:
  - secretRef: jwt-secrets
  - secretRef: common-secrets
  - secretRef: auth-database-secret

env:
  ASPNETCORE_ENVIRONMENT: "Production"
  ASPNETCORE_URLS: "http://+:6023"
  ASPNETCORE_HTTP_PORTS: "6023"
  ASPNETCORE_HTTPS_PORTS: "6024"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi